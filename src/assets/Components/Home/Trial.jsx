import React from 'react';

const Trial = () => {
    return (
        <div>
            <button className='btn focus:bg-special-500 focus:text-white'> {info.Category} </button>
            to={`/${product_id}`}
        </div>
    );
};

export default Trial;